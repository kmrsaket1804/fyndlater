import { ComingSoon } from '@/components/dashboard/coming-soon';

export default function AllSavesPage() {
  return (
    <ComingSoon
      title="All Saves"
      description="Browse every reel, post, link, and screenshot you've sent to Faye — filterable by type, date, and collection."
    />
  );
}
